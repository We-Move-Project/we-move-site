---
type Props = {
  placeholder: string;
  src?: string;
};

const { placeholder, src } = Astro.props;
---

{src ? (
  <img class="image-slot-img" src={src} alt={placeholder} loading="lazy" />
) : (
  <div class="image-slot" role="img" aria-label={placeholder}>{placeholder}</div>
)}
